name: CPI Download to GitHub

on:
  workflow_dispatch:
    inputs:
      integrationFlowId:
        description: "Enter CPI iFlow ID, example: ExternalLoggingPOC"
        required: true
        type: string

permissions:
  contents: write

jobs:
  download:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Piper
        run: |
          curl -L https://github.com/SAP/jenkins-library/releases/latest/download/piper -o piper
          chmod +x piper

      - name: Download CPI Artifact
        env:
          PIPER_apiServiceKey: ${{ secrets.PIPER_apiServiceKey }}
        run: |
          ./piper integrationArtifactDownload \
            --integrationFlowId "${{ github.event.inputs.integrationFlowId }}" \
            --integrationFlowVersion "active" \
            --downloadPath "./cpi-artifacts"

      - name: Commit and Push to Repo
        run: |
          git config user.name "cpi-bot"
          git config user.email "cpi-bot@github.com"

          rm -f piper

          git add cpi-artifacts/
          git commit -m "Backup ${{ github.event.inputs.integrationFlowId }} from SAP CPI" || echo "No changes"

          git push
